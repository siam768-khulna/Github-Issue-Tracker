This page live link : https://siam768-khulna.github.io/Github-Issue-Tracker/

1. What is the difference between var, let and const?
    Answer: var, let and const are used to declare variables in JavaScript.
var:It is the old way to declare variables. It can be re-declared and re-assigned.
let: It cannot be re-declared, but the value can be re-assigned.
const: It cannot be re-declared or re-assigned. The value remains constant after declaration.

2. What is the Spread Operator (...)?
   The spread operator (...) is used to expand or copy elements of an array or object
3. What is the difference between map(), filter(), and forEach()?
   ANSWER: map(): It creates a new array by applying a function for eaxh element of the original array.
    filter(): It creates a new array with elements that match a condition.
    forEach(): It is used to loop through an array, but it does not return a new array.
4. What is an arrow function?
   Answer: An arrow function is a shorter way to write function in JavaScrip using =>
5. What are template literals?
   Answer: Template literals are used to create string using backticks ` `.
